# Распознавание: onnx-asr вместо faster-whisper (PLAN, этап 2). Уходит
# CTranslate2, а вместе с ним обязательная CUDA: GigaAM v3 на процессоре
# укладывается в секунду. Установщик худеет с 660 МБ до десятков, модель -
# с 1.6 ГБ до 226 МБ.
onnx-asr>=0.12
# Верхняя граница - не осторожность, а замер: с 1.25.0 целочисленные ядра
# считают вдвое дольше, а мы возим модель именно int8. test.wav (7.8 с),
# процессор, 2 потока, медиана, отдельный чистый venv на каждую версию:
#
#     1.24.4  0.236 с       1.26.0  0.401 с  (1.70x)
#     1.25.0  0.425 с       1.27.0  0.439 с  (1.86x)
#     1.25.1  0.410 с
#
# Дело не в наших настройках: разрыв держится на 2/3/4 потоках и с
# force_spinning_stop, и без него. Дело именно в int8 - на fp32-модели
# разницы между 1.24.4 и 1.27.0 нет вовсе (0.530 против 0.528), а
# просаживаются все четыре наши int8-модели, и RNN-T, и CTC. Текст при этом
# везде побайтово тот же: платим только временем.
#
# Заодно сходится с колесом DirectML - у него 1.24.4 и есть последнее.
# Снимать границу - когда выйдет версия, на которой замер вернётся к 0.24 с.
onnxruntime>=1.24.4,<1.25
# Качает модели с HuggingFace и сам сверяет их целостность по sha-блобам.
huggingface_hub>=0.30
# Интерфейс: оверлей, настройки и трей (PLAN, этапы 6-7). LGPLv3 - коммерческая
# лицензия не нужна, но раздавать только onedir: в onefile Qt запечатан, и §4(d)
# о перелинковке нарушается. Qt Graphs, Quick 3D и Quick Timeline не трогать:
# они GPL-only и утянут проект в GPL.
PySide6>=6.9
sounddevice>=0.5
# Только Windows. У автора в описании репозитория буквально «Hook and simulate
# global keyboard events on Windows and Linux», macOS помечена experimental, а
# сам проект - unmaintained с марта 2020. На Mac клавиатурный слой придётся
# делать на pynput, который у нас уже есть и уже держит мышь.
keyboard>=0.13; sys_platform == "win32"
# Кнопки мыши как хоткей (ctrl+mouse4): keyboard про мышь не знает.
# Была библиотека mouse - выброшена (PLAN 0.4): автор архивировал её в феврале
# 2026, последний коммит - январь 2020, и подавлять события она не умела вовсе.
# Из-за этого боковая кнопка долетала до чужого окна, а в Chromium и Electron
# X1 - это «Назад»: поле ввода очищалось прямо посреди диктовки.
pynput>=1.7
# pystray ушёл вместе с этапом 7: трей теперь QSystemTrayIcon, и своего потока
# ему не нужно. Заодно из сборки пропала вкомпиленная LGPLv3-библиотека.
# Pillow остаётся: на нём иконка трея (icons.py).
pillow>=10
# Только Windows, очевидно. Без маркера `pip install -r requirements.txt` на
# Mac обрывался на этой строке - то есть до проб этапа 20 нельзя было дойти
# вовсе, хотя всё остальное ставится колёсами и компилятора не требует.
pywin32>=306; sys_platform == "win32"
numpy
# Расшифровка готового файла (PLAN 11): чем открывать чужую запись.
#
# Выбрано замером, а не по вкусу - таблица и цифры в шапке audiofile.py.
# Коротко: установщик 70 МБ, и PyAV (он же ffmpeg, единственный, кто берёт всё
# сразу) добавил бы к нему 65.9 МБ - почти вдвое. libsndfile закрывает opus,
# то есть голосовые из телеграма, за 3.59 МБ; AAC (.m4a) он не знает вовсе, и
# его берёт Media Foundation, которая уже стоит в каждой Windows и весит ноль.
soundfile>=0.13
# Пересчёт частоты и сведение в моно: 3.59 МБ libsndfile отдаёт запись как
# есть, а модели нужны ровно 16 кГц моно. Свой пересчёт писать не стали -
# антиалиасинг на 44.1 -> 16 кГц это не тридцать строк, а место, где ошибка
# тихо портит распознавание, и заметить её можно только по проценту ошибок.
miniaudio>=1.59
# Видеокарта не нужна. Кому хочется гонять Whisper на GPU - ставит вместо
# onnxruntime колесо onnxruntime-gpu, и device=auto подхватит CUDA сам.
