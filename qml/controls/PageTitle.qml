// Заголовок страницы. Дисплейный кегль по канону Korti (app-shell, h1):
// 26px, трекинг -.02em - так задан голос бренда, и мельче он перестаёт быть
// заголовком. Вес 700, а не канонические 800: Onest (кириллическая замена
// Hanken Grotesk) в ExtraBold заметно чернее оригинала и читается тяжело -
// на живом экране 800 придавливал страницу. 700 держит вес заголовка, не
// перегружая.
import QtQuick

Text {
    font.family: T.sans
    // Кегль токеном, а не числом: 26 стояло здесь единственным сырым размером
    // во всём qml/, и любая правка шкалы обходила заголовок стороной.
    font.pixelSize: T.tDisplay
    font.weight: Font.Bold
    // Трекинг считаем от СВОЕГО кегля, а не от токена и не готовым числом.
    //
    // -.02em - тот приём, на котором держится «дорогой» заголовок: буквы на
    // крупном кегле расставлены с запасом под мелкий текст, и без сжатия строка
    // выглядит разболтанной. Прежнее -0.52 было тем же -.02em, но от 26 - при
    // первой же смене кегля оно стало бы неправдой молча.
    //
    // От `font.pixelSize`, а не от `T.tDisplay`, - чтобы заголовок можно было
    // переопределить кеглем и получить правильный трекинг даром. Этим и живут
    // шаги мастера: они набраны 20-м, и до сих пор каждый из семи объявлял
    // шрифт заново и трекинга не имел вовсе.
    font.letterSpacing: -0.02 * font.pixelSize
    color: T.text
}
